# stages:
#   - build
#   - deploy

# variables:
#   DOCKER_TLS_CERTDIR: "/certs"

# # ── Build stage ───────────────────────────────────────────────
# # Builds Docker images and pushes to GitLab Container Registry.
# # Runs on pushes to main or tags.

# .build-template: &build-template
#   stage: build
#   image: docker:27
#   services:
#     - docker:27-dind
#   before_script:
#     - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
#   script:
#     - |
#       TAG="${CI_COMMIT_TAG:-$CI_COMMIT_SHORT_SHA}"
#       IMAGE="$CI_REGISTRY_IMAGE/$SERVICE_NAME"

#       docker build -t "$IMAGE:$TAG" -t "$IMAGE:latest" "./$SERVICE_NAME"
#       docker push "$IMAGE:$TAG"
#       docker push "$IMAGE:latest"
#   rules:
#     - if: $CI_COMMIT_BRANCH == "main"
#     - if: $CI_COMMIT_TAG

# build-auth-service:
#   <<: *build-template
#   variables:
#     SERVICE_NAME: auth-service

# build-user-service:
#   <<: *build-template
#   variables:
#     SERVICE_NAME: user-service

# build-notification-service:
#   <<: *build-template
#   variables:
#     SERVICE_NAME: notification-service

# build-graphql-gateway:
#   <<: *build-template
#   variables:
#     SERVICE_NAME: graphql-gateway

# # ── Deploy stage ──────────────────────────────────────────────
# # SSHs into VPS, pulls latest images, restarts services.
# # Runs only on main branch or tags.

# deploy-production:
#   stage: deploy
#   image: alpine:3.20
#   before_script:
#     - apk add --no-cache openssh-client
#     - eval $(ssh-agent -s)
#     - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
#     - mkdir -p ~/.ssh
#     - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
#     - chmod 644 ~/.ssh/known_hosts
#   script:
#     - |
#       ssh "$SSH_USER@$SSH_HOST" << 'ENDSSH'
#         cd ~/microservices-arbex

#         # Login to GitLab registry
#         docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"

#         # Pull latest images
#         docker compose pull

#         # Restart services (infra stays up, only app containers recreated)
#         docker compose up -d --remove-orphans

#         # Cleanup old images
#         docker image prune -f
#       ENDSSH
#   rules:
#     - if: $CI_COMMIT_BRANCH == "main"
#     - if: $CI_COMMIT_TAG
#   environment:
#     name: production
